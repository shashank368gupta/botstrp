<div id="<%= dom_id post %>">
  <p>
    <strong>Name:</strong>
    <%= post.name %>
  </p>

  <p>
    <% if post.avtar.attached? %>
       <image src="<%= (url_for(post.avtar))%>">
        <%end%>
  </p>

</div>
